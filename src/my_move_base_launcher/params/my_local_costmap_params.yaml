global_frame: odom
rolling_window: true

plugins:
  - {name: obstacles_laser,           type: "costmap_2d::ObstacleLayer"}
  - {name: inflation,                 type: "costmap_2d::InflationLayer"}


local_costmap:
  width: 5.0
  height: 5.0
  update_frequency: 1.0  # Set the update frequency to 1 Hz (slower rate)
